spring:
  datasource:
    url: jdbc:p6spy:mariadb://localhost:3308/hubdb?useUnicode=true&characterEncoding=utf8
    username: hub
    password: pass
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        format_sql: true
    show-sql: true

logging:
  file:
    prefix: LOCAL